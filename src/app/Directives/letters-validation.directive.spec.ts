import { LettersValidationDirective } from './letters-validation.directive';

describe('LettersValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new LettersValidationDirective();
    expect(directive).toBeTruthy();
  });
});
